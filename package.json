{
  "name": "drugpedia",
  "private": true,
  "description": "Harm reduction reference site - dev tooling only, the site itself is buildless",
  "scripts": {
    "build:data": "node scripts/fetch-tripsit.js && node scripts/build-data.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^10.0.0",
    "globals": "^16.0.0",
    "prettier": "^3.0.0"
  }
}
